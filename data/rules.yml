version: "3.1"

rules:
# - rule: Ask for name at the beginning of chat
#   steps:
  # - intent: greet  # This can be an intent like `greet` or a generic start intent
  # - action: utter_ask_name

- rule: Protective Session Start
  steps:
  - action: action_session_start
  # - action: action_ask_name
  - action: action_greet_fullname

# - rule: ask company name
#   steps:
#   - action: action_set_company_name
